# 이 저장소에서 일하는 법

일본 여행 회화 학습앱. 3,000문장 60단계. 자세한 것은 `docs/README.md` 부터 읽는다.

---

## 시작할 때

`docs/README.md` 가 문서 지도다. 손댈 자리에 맞는 문서를 먼저 읽는다.

| 손댈 곳 | 먼저 읽을 것 |
|---|---|
| 문장·사전 | `docs/PLAN.md` |
| 회차·가림·별 | `docs/ROUNDS.md` |
| 더 배워보기 | `docs/NOTES-PLAN.md` |
| 음성 | `docs/VOICE.md` |
| 다음에 뭘 할지 | `docs/BACKLOG.md` |

---

## 고치는 흐름

```bash
# 1. src/ 안의 파일을 고친다 (dist/ 를 직접 고치지 않는다)
node tools/check.js          # 검사 — 반드시
node tools/quiz-audit.js     # 보기를 손댔다면
python tools/build.py        # src → dist/jp3000.html + sw.js 판 찍기
git add -A && git commit && git push     # Pages 가 main 을 그대로 낸다
```

**빌드하지 않으면 폰에 아무것도 안 간다.** 「고쳤는데 화면이 그대로다」의
첫 번째 원인은 늘 이것이다. 두 번째는 푸시를 안 한 것이다.
설정 맨 아래 「지금 판」 숫자로 확인한다.

---

## 기록한다

**무엇을 하든 `docs/` 에 남긴다.** 규칙은 `docs/README.md` 의 「기록 규칙」에 있다.
요약하면 —

- 고쳤으면 `CHANGELOG.md` 맨 위에. **증상이 아니라 원인을 적는다**
- 두 길 중 하나를 골랐으면 **버린 길과 이유까지** 적는다
- 안 되는 것을 확인했으면 그 분야 문서의 「안 되는 것」 칸에.
  적어 두지 않으면 다음에 같은 길을 또 판다 — 실제로 그랬다
- 할 일이 생기면 `BACKLOG.md`, 끝내면 지우고 `CHANGELOG.md` 로
- 「많다」 대신 숫자를. 「빠를 것이다」 대신 잰 값을

---

## 지키는 것

- **키를 화면에 찍지 않는다.** `API_key.txt` · `.gemini-key` · `.env` 는
  `.gitignore` 에 있다. 커밋 전에 딸려 들어가지 않았는지 본다
- **런타임에 AI 를 부르지 않는다.** 정적 배포라 키를 숨길 데가 없고
  오프라인에서 못 쓴다. 설명도 음성도 빌드 타임에 미리 만든다
- **`dist/voice/` 는 저장소 밖이다.** 무게 때문이다 (`docs/VOICE.md` 5장)
- **큰 구간을 정규식으로 치환하지 않는다.** 범위를 잘못 잡아 함수 여럿이
  통째로 사라진 적이 있다. 정확한 문자열로 다루고, 고친 뒤 렌더링을 확인한다
- **검사기도 틀린다.** 경고가 뜨면 문장을 먼저 의심하되, 문장이 맞으면 규칙을 의심한다

---

## 글투

화면에 나오는 글과 주석은 **쉬운 우리말**로 쓴다.
「팝업이 열리면 뒤를 흐린다」처럼, 무엇을 왜 하는지가 한 번에 읽히게.
주석은 코드가 하는 일을 옮겨 적지 않고 **왜 그렇게 했는지**를 적는다.
